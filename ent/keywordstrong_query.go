// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/wolodata/schema/ent/keywordstrong"
	"github.com/wolodata/schema/ent/predicate"
)

// KeywordStrongQuery is the builder for querying KeywordStrong entities.
type KeywordStrongQuery struct {
	config
	ctx        *QueryContext
	order      []keywordstrong.OrderOption
	inters     []Interceptor
	predicates []predicate.KeywordStrong
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KeywordStrongQuery builder.
func (ksq *KeywordStrongQuery) Where(ps ...predicate.KeywordStrong) *KeywordStrongQuery {
	ksq.predicates = append(ksq.predicates, ps...)
	return ksq
}

// Limit the number of records to be returned by this query.
func (ksq *KeywordStrongQuery) Limit(limit int) *KeywordStrongQuery {
	ksq.ctx.Limit = &limit
	return ksq
}

// Offset to start from.
func (ksq *KeywordStrongQuery) Offset(offset int) *KeywordStrongQuery {
	ksq.ctx.Offset = &offset
	return ksq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ksq *KeywordStrongQuery) Unique(unique bool) *KeywordStrongQuery {
	ksq.ctx.Unique = &unique
	return ksq
}

// Order specifies how the records should be ordered.
func (ksq *KeywordStrongQuery) Order(o ...keywordstrong.OrderOption) *KeywordStrongQuery {
	ksq.order = append(ksq.order, o...)
	return ksq
}

// First returns the first KeywordStrong entity from the query.
// Returns a *NotFoundError when no KeywordStrong was found.
func (ksq *KeywordStrongQuery) First(ctx context.Context) (*KeywordStrong, error) {
	nodes, err := ksq.Limit(1).All(setContextOp(ctx, ksq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{keywordstrong.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ksq *KeywordStrongQuery) FirstX(ctx context.Context) *KeywordStrong {
	node, err := ksq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first KeywordStrong ID from the query.
// Returns a *NotFoundError when no KeywordStrong ID was found.
func (ksq *KeywordStrongQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ksq.Limit(1).IDs(setContextOp(ctx, ksq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{keywordstrong.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ksq *KeywordStrongQuery) FirstIDX(ctx context.Context) string {
	id, err := ksq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single KeywordStrong entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one KeywordStrong entity is found.
// Returns a *NotFoundError when no KeywordStrong entities are found.
func (ksq *KeywordStrongQuery) Only(ctx context.Context) (*KeywordStrong, error) {
	nodes, err := ksq.Limit(2).All(setContextOp(ctx, ksq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{keywordstrong.Label}
	default:
		return nil, &NotSingularError{keywordstrong.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ksq *KeywordStrongQuery) OnlyX(ctx context.Context) *KeywordStrong {
	node, err := ksq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only KeywordStrong ID in the query.
// Returns a *NotSingularError when more than one KeywordStrong ID is found.
// Returns a *NotFoundError when no entities are found.
func (ksq *KeywordStrongQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ksq.Limit(2).IDs(setContextOp(ctx, ksq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{keywordstrong.Label}
	default:
		err = &NotSingularError{keywordstrong.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ksq *KeywordStrongQuery) OnlyIDX(ctx context.Context) string {
	id, err := ksq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of KeywordStrongs.
func (ksq *KeywordStrongQuery) All(ctx context.Context) ([]*KeywordStrong, error) {
	ctx = setContextOp(ctx, ksq.ctx, "All")
	if err := ksq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*KeywordStrong, *KeywordStrongQuery]()
	return withInterceptors[[]*KeywordStrong](ctx, ksq, qr, ksq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ksq *KeywordStrongQuery) AllX(ctx context.Context) []*KeywordStrong {
	nodes, err := ksq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of KeywordStrong IDs.
func (ksq *KeywordStrongQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ksq.ctx.Unique == nil && ksq.path != nil {
		ksq.Unique(true)
	}
	ctx = setContextOp(ctx, ksq.ctx, "IDs")
	if err = ksq.Select(keywordstrong.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ksq *KeywordStrongQuery) IDsX(ctx context.Context) []string {
	ids, err := ksq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ksq *KeywordStrongQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ksq.ctx, "Count")
	if err := ksq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ksq, querierCount[*KeywordStrongQuery](), ksq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ksq *KeywordStrongQuery) CountX(ctx context.Context) int {
	count, err := ksq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ksq *KeywordStrongQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ksq.ctx, "Exist")
	switch _, err := ksq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ksq *KeywordStrongQuery) ExistX(ctx context.Context) bool {
	exist, err := ksq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KeywordStrongQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ksq *KeywordStrongQuery) Clone() *KeywordStrongQuery {
	if ksq == nil {
		return nil
	}
	return &KeywordStrongQuery{
		config:     ksq.config,
		ctx:        ksq.ctx.Clone(),
		order:      append([]keywordstrong.OrderOption{}, ksq.order...),
		inters:     append([]Interceptor{}, ksq.inters...),
		predicates: append([]predicate.KeywordStrong{}, ksq.predicates...),
		// clone intermediate query.
		sql:  ksq.sql.Clone(),
		path: ksq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Category uint64 `json:"category,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.KeywordStrong.Query().
//		GroupBy(keywordstrong.FieldCategory).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ksq *KeywordStrongQuery) GroupBy(field string, fields ...string) *KeywordStrongGroupBy {
	ksq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &KeywordStrongGroupBy{build: ksq}
	grbuild.flds = &ksq.ctx.Fields
	grbuild.label = keywordstrong.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Category uint64 `json:"category,omitempty"`
//	}
//
//	client.KeywordStrong.Query().
//		Select(keywordstrong.FieldCategory).
//		Scan(ctx, &v)
func (ksq *KeywordStrongQuery) Select(fields ...string) *KeywordStrongSelect {
	ksq.ctx.Fields = append(ksq.ctx.Fields, fields...)
	sbuild := &KeywordStrongSelect{KeywordStrongQuery: ksq}
	sbuild.label = keywordstrong.Label
	sbuild.flds, sbuild.scan = &ksq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a KeywordStrongSelect configured with the given aggregations.
func (ksq *KeywordStrongQuery) Aggregate(fns ...AggregateFunc) *KeywordStrongSelect {
	return ksq.Select().Aggregate(fns...)
}

func (ksq *KeywordStrongQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ksq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ksq); err != nil {
				return err
			}
		}
	}
	for _, f := range ksq.ctx.Fields {
		if !keywordstrong.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ksq.path != nil {
		prev, err := ksq.path(ctx)
		if err != nil {
			return err
		}
		ksq.sql = prev
	}
	return nil
}

func (ksq *KeywordStrongQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*KeywordStrong, error) {
	var (
		nodes = []*KeywordStrong{}
		_spec = ksq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*KeywordStrong).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &KeywordStrong{config: ksq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ksq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ksq *KeywordStrongQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ksq.querySpec()
	_spec.Node.Columns = ksq.ctx.Fields
	if len(ksq.ctx.Fields) > 0 {
		_spec.Unique = ksq.ctx.Unique != nil && *ksq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ksq.driver, _spec)
}

func (ksq *KeywordStrongQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(keywordstrong.Table, keywordstrong.Columns, sqlgraph.NewFieldSpec(keywordstrong.FieldID, field.TypeString))
	_spec.From = ksq.sql
	if unique := ksq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ksq.path != nil {
		_spec.Unique = true
	}
	if fields := ksq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, keywordstrong.FieldID)
		for i := range fields {
			if fields[i] != keywordstrong.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ksq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ksq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ksq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ksq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ksq *KeywordStrongQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ksq.driver.Dialect())
	t1 := builder.Table(keywordstrong.Table)
	columns := ksq.ctx.Fields
	if len(columns) == 0 {
		columns = keywordstrong.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ksq.sql != nil {
		selector = ksq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ksq.ctx.Unique != nil && *ksq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ksq.predicates {
		p(selector)
	}
	for _, p := range ksq.order {
		p(selector)
	}
	if offset := ksq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ksq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// KeywordStrongGroupBy is the group-by builder for KeywordStrong entities.
type KeywordStrongGroupBy struct {
	selector
	build *KeywordStrongQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ksgb *KeywordStrongGroupBy) Aggregate(fns ...AggregateFunc) *KeywordStrongGroupBy {
	ksgb.fns = append(ksgb.fns, fns...)
	return ksgb
}

// Scan applies the selector query and scans the result into the given value.
func (ksgb *KeywordStrongGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ksgb.build.ctx, "GroupBy")
	if err := ksgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KeywordStrongQuery, *KeywordStrongGroupBy](ctx, ksgb.build, ksgb, ksgb.build.inters, v)
}

func (ksgb *KeywordStrongGroupBy) sqlScan(ctx context.Context, root *KeywordStrongQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ksgb.fns))
	for _, fn := range ksgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ksgb.flds)+len(ksgb.fns))
		for _, f := range *ksgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ksgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ksgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// KeywordStrongSelect is the builder for selecting fields of KeywordStrong entities.
type KeywordStrongSelect struct {
	*KeywordStrongQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (kss *KeywordStrongSelect) Aggregate(fns ...AggregateFunc) *KeywordStrongSelect {
	kss.fns = append(kss.fns, fns...)
	return kss
}

// Scan applies the selector query and scans the result into the given value.
func (kss *KeywordStrongSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, kss.ctx, "Select")
	if err := kss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KeywordStrongQuery, *KeywordStrongSelect](ctx, kss.KeywordStrongQuery, kss, kss.inters, v)
}

func (kss *KeywordStrongSelect) sqlScan(ctx context.Context, root *KeywordStrongQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(kss.fns))
	for _, fn := range kss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*kss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
