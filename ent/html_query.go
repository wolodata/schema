// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/wolodata/schema/ent/html"
	"github.com/wolodata/schema/ent/predicate"
)

// HTMLQuery is the builder for querying Html entities.
type HTMLQuery struct {
	config
	ctx        *QueryContext
	order      []html.OrderOption
	inters     []Interceptor
	predicates []predicate.Html
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HTMLQuery builder.
func (hq *HTMLQuery) Where(ps ...predicate.Html) *HTMLQuery {
	hq.predicates = append(hq.predicates, ps...)
	return hq
}

// Limit the number of records to be returned by this query.
func (hq *HTMLQuery) Limit(limit int) *HTMLQuery {
	hq.ctx.Limit = &limit
	return hq
}

// Offset to start from.
func (hq *HTMLQuery) Offset(offset int) *HTMLQuery {
	hq.ctx.Offset = &offset
	return hq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hq *HTMLQuery) Unique(unique bool) *HTMLQuery {
	hq.ctx.Unique = &unique
	return hq
}

// Order specifies how the records should be ordered.
func (hq *HTMLQuery) Order(o ...html.OrderOption) *HTMLQuery {
	hq.order = append(hq.order, o...)
	return hq
}

// First returns the first Html entity from the query.
// Returns a *NotFoundError when no Html was found.
func (hq *HTMLQuery) First(ctx context.Context) (*Html, error) {
	nodes, err := hq.Limit(1).All(setContextOp(ctx, hq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{html.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hq *HTMLQuery) FirstX(ctx context.Context) *Html {
	node, err := hq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Html ID from the query.
// Returns a *NotFoundError when no Html ID was found.
func (hq *HTMLQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = hq.Limit(1).IDs(setContextOp(ctx, hq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{html.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hq *HTMLQuery) FirstIDX(ctx context.Context) int64 {
	id, err := hq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Html entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Html entity is found.
// Returns a *NotFoundError when no Html entities are found.
func (hq *HTMLQuery) Only(ctx context.Context) (*Html, error) {
	nodes, err := hq.Limit(2).All(setContextOp(ctx, hq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{html.Label}
	default:
		return nil, &NotSingularError{html.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hq *HTMLQuery) OnlyX(ctx context.Context) *Html {
	node, err := hq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Html ID in the query.
// Returns a *NotSingularError when more than one Html ID is found.
// Returns a *NotFoundError when no entities are found.
func (hq *HTMLQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = hq.Limit(2).IDs(setContextOp(ctx, hq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{html.Label}
	default:
		err = &NotSingularError{html.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hq *HTMLQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := hq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Htmls.
func (hq *HTMLQuery) All(ctx context.Context) ([]*Html, error) {
	ctx = setContextOp(ctx, hq.ctx, "All")
	if err := hq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Html, *HTMLQuery]()
	return withInterceptors[[]*Html](ctx, hq, qr, hq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hq *HTMLQuery) AllX(ctx context.Context) []*Html {
	nodes, err := hq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Html IDs.
func (hq *HTMLQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if hq.ctx.Unique == nil && hq.path != nil {
		hq.Unique(true)
	}
	ctx = setContextOp(ctx, hq.ctx, "IDs")
	if err = hq.Select(html.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hq *HTMLQuery) IDsX(ctx context.Context) []int64 {
	ids, err := hq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hq *HTMLQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hq.ctx, "Count")
	if err := hq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hq, querierCount[*HTMLQuery](), hq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hq *HTMLQuery) CountX(ctx context.Context) int {
	count, err := hq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hq *HTMLQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hq.ctx, "Exist")
	switch _, err := hq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hq *HTMLQuery) ExistX(ctx context.Context) bool {
	exist, err := hq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HTMLQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hq *HTMLQuery) Clone() *HTMLQuery {
	if hq == nil {
		return nil
	}
	return &HTMLQuery{
		config:     hq.config,
		ctx:        hq.ctx.Clone(),
		order:      append([]html.OrderOption{}, hq.order...),
		inters:     append([]Interceptor{}, hq.inters...),
		predicates: append([]predicate.Html{}, hq.predicates...),
		// clone intermediate query.
		sql:  hq.sql.Clone(),
		path: hq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OriginShortID string `json:"origin_short_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HTML.Query().
//		GroupBy(html.FieldOriginShortID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hq *HTMLQuery) GroupBy(field string, fields ...string) *HTMLGroupBy {
	hq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HTMLGroupBy{build: hq}
	grbuild.flds = &hq.ctx.Fields
	grbuild.label = html.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OriginShortID string `json:"origin_short_id,omitempty"`
//	}
//
//	client.HTML.Query().
//		Select(html.FieldOriginShortID).
//		Scan(ctx, &v)
func (hq *HTMLQuery) Select(fields ...string) *HTMLSelect {
	hq.ctx.Fields = append(hq.ctx.Fields, fields...)
	sbuild := &HTMLSelect{HTMLQuery: hq}
	sbuild.label = html.Label
	sbuild.flds, sbuild.scan = &hq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HTMLSelect configured with the given aggregations.
func (hq *HTMLQuery) Aggregate(fns ...AggregateFunc) *HTMLSelect {
	return hq.Select().Aggregate(fns...)
}

func (hq *HTMLQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hq); err != nil {
				return err
			}
		}
	}
	for _, f := range hq.ctx.Fields {
		if !html.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hq.path != nil {
		prev, err := hq.path(ctx)
		if err != nil {
			return err
		}
		hq.sql = prev
	}
	return nil
}

func (hq *HTMLQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Html, error) {
	var (
		nodes = []*Html{}
		_spec = hq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Html).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Html{config: hq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hq *HTMLQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hq.querySpec()
	_spec.Node.Columns = hq.ctx.Fields
	if len(hq.ctx.Fields) > 0 {
		_spec.Unique = hq.ctx.Unique != nil && *hq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hq.driver, _spec)
}

func (hq *HTMLQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(html.Table, html.Columns, sqlgraph.NewFieldSpec(html.FieldID, field.TypeInt64))
	_spec.From = hq.sql
	if unique := hq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hq.path != nil {
		_spec.Unique = true
	}
	if fields := hq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, html.FieldID)
		for i := range fields {
			if fields[i] != html.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hq *HTMLQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hq.driver.Dialect())
	t1 := builder.Table(html.Table)
	columns := hq.ctx.Fields
	if len(columns) == 0 {
		columns = html.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hq.sql != nil {
		selector = hq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hq.ctx.Unique != nil && *hq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hq.predicates {
		p(selector)
	}
	for _, p := range hq.order {
		p(selector)
	}
	if offset := hq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HTMLGroupBy is the group-by builder for Html entities.
type HTMLGroupBy struct {
	selector
	build *HTMLQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hgb *HTMLGroupBy) Aggregate(fns ...AggregateFunc) *HTMLGroupBy {
	hgb.fns = append(hgb.fns, fns...)
	return hgb
}

// Scan applies the selector query and scans the result into the given value.
func (hgb *HTMLGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hgb.build.ctx, "GroupBy")
	if err := hgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HTMLQuery, *HTMLGroupBy](ctx, hgb.build, hgb, hgb.build.inters, v)
}

func (hgb *HTMLGroupBy) sqlScan(ctx context.Context, root *HTMLQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hgb.fns))
	for _, fn := range hgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hgb.flds)+len(hgb.fns))
		for _, f := range *hgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HTMLSelect is the builder for selecting fields of HTML entities.
type HTMLSelect struct {
	*HTMLQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hs *HTMLSelect) Aggregate(fns ...AggregateFunc) *HTMLSelect {
	hs.fns = append(hs.fns, fns...)
	return hs
}

// Scan applies the selector query and scans the result into the given value.
func (hs *HTMLSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hs.ctx, "Select")
	if err := hs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HTMLQuery, *HTMLSelect](ctx, hs.HTMLQuery, hs, hs.inters, v)
}

func (hs *HTMLSelect) sqlScan(ctx context.Context, root *HTMLQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hs.fns))
	for _, fn := range hs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
