// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/wolodata/schema/ent/keywordweak"
	"github.com/wolodata/schema/ent/predicate"
)

// KeywordWeakQuery is the builder for querying KeywordWeak entities.
type KeywordWeakQuery struct {
	config
	ctx        *QueryContext
	order      []keywordweak.OrderOption
	inters     []Interceptor
	predicates []predicate.KeywordWeak
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KeywordWeakQuery builder.
func (kwq *KeywordWeakQuery) Where(ps ...predicate.KeywordWeak) *KeywordWeakQuery {
	kwq.predicates = append(kwq.predicates, ps...)
	return kwq
}

// Limit the number of records to be returned by this query.
func (kwq *KeywordWeakQuery) Limit(limit int) *KeywordWeakQuery {
	kwq.ctx.Limit = &limit
	return kwq
}

// Offset to start from.
func (kwq *KeywordWeakQuery) Offset(offset int) *KeywordWeakQuery {
	kwq.ctx.Offset = &offset
	return kwq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (kwq *KeywordWeakQuery) Unique(unique bool) *KeywordWeakQuery {
	kwq.ctx.Unique = &unique
	return kwq
}

// Order specifies how the records should be ordered.
func (kwq *KeywordWeakQuery) Order(o ...keywordweak.OrderOption) *KeywordWeakQuery {
	kwq.order = append(kwq.order, o...)
	return kwq
}

// First returns the first KeywordWeak entity from the query.
// Returns a *NotFoundError when no KeywordWeak was found.
func (kwq *KeywordWeakQuery) First(ctx context.Context) (*KeywordWeak, error) {
	nodes, err := kwq.Limit(1).All(setContextOp(ctx, kwq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{keywordweak.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (kwq *KeywordWeakQuery) FirstX(ctx context.Context) *KeywordWeak {
	node, err := kwq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first KeywordWeak ID from the query.
// Returns a *NotFoundError when no KeywordWeak ID was found.
func (kwq *KeywordWeakQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = kwq.Limit(1).IDs(setContextOp(ctx, kwq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{keywordweak.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (kwq *KeywordWeakQuery) FirstIDX(ctx context.Context) string {
	id, err := kwq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single KeywordWeak entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one KeywordWeak entity is found.
// Returns a *NotFoundError when no KeywordWeak entities are found.
func (kwq *KeywordWeakQuery) Only(ctx context.Context) (*KeywordWeak, error) {
	nodes, err := kwq.Limit(2).All(setContextOp(ctx, kwq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{keywordweak.Label}
	default:
		return nil, &NotSingularError{keywordweak.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (kwq *KeywordWeakQuery) OnlyX(ctx context.Context) *KeywordWeak {
	node, err := kwq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only KeywordWeak ID in the query.
// Returns a *NotSingularError when more than one KeywordWeak ID is found.
// Returns a *NotFoundError when no entities are found.
func (kwq *KeywordWeakQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = kwq.Limit(2).IDs(setContextOp(ctx, kwq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{keywordweak.Label}
	default:
		err = &NotSingularError{keywordweak.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (kwq *KeywordWeakQuery) OnlyIDX(ctx context.Context) string {
	id, err := kwq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of KeywordWeaks.
func (kwq *KeywordWeakQuery) All(ctx context.Context) ([]*KeywordWeak, error) {
	ctx = setContextOp(ctx, kwq.ctx, "All")
	if err := kwq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*KeywordWeak, *KeywordWeakQuery]()
	return withInterceptors[[]*KeywordWeak](ctx, kwq, qr, kwq.inters)
}

// AllX is like All, but panics if an error occurs.
func (kwq *KeywordWeakQuery) AllX(ctx context.Context) []*KeywordWeak {
	nodes, err := kwq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of KeywordWeak IDs.
func (kwq *KeywordWeakQuery) IDs(ctx context.Context) (ids []string, err error) {
	if kwq.ctx.Unique == nil && kwq.path != nil {
		kwq.Unique(true)
	}
	ctx = setContextOp(ctx, kwq.ctx, "IDs")
	if err = kwq.Select(keywordweak.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (kwq *KeywordWeakQuery) IDsX(ctx context.Context) []string {
	ids, err := kwq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (kwq *KeywordWeakQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, kwq.ctx, "Count")
	if err := kwq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, kwq, querierCount[*KeywordWeakQuery](), kwq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (kwq *KeywordWeakQuery) CountX(ctx context.Context) int {
	count, err := kwq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (kwq *KeywordWeakQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, kwq.ctx, "Exist")
	switch _, err := kwq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (kwq *KeywordWeakQuery) ExistX(ctx context.Context) bool {
	exist, err := kwq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KeywordWeakQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (kwq *KeywordWeakQuery) Clone() *KeywordWeakQuery {
	if kwq == nil {
		return nil
	}
	return &KeywordWeakQuery{
		config:     kwq.config,
		ctx:        kwq.ctx.Clone(),
		order:      append([]keywordweak.OrderOption{}, kwq.order...),
		inters:     append([]Interceptor{}, kwq.inters...),
		predicates: append([]predicate.KeywordWeak{}, kwq.predicates...),
		// clone intermediate query.
		sql:  kwq.sql.Clone(),
		path: kwq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Category uint64 `json:"category,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.KeywordWeak.Query().
//		GroupBy(keywordweak.FieldCategory).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (kwq *KeywordWeakQuery) GroupBy(field string, fields ...string) *KeywordWeakGroupBy {
	kwq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &KeywordWeakGroupBy{build: kwq}
	grbuild.flds = &kwq.ctx.Fields
	grbuild.label = keywordweak.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Category uint64 `json:"category,omitempty"`
//	}
//
//	client.KeywordWeak.Query().
//		Select(keywordweak.FieldCategory).
//		Scan(ctx, &v)
func (kwq *KeywordWeakQuery) Select(fields ...string) *KeywordWeakSelect {
	kwq.ctx.Fields = append(kwq.ctx.Fields, fields...)
	sbuild := &KeywordWeakSelect{KeywordWeakQuery: kwq}
	sbuild.label = keywordweak.Label
	sbuild.flds, sbuild.scan = &kwq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a KeywordWeakSelect configured with the given aggregations.
func (kwq *KeywordWeakQuery) Aggregate(fns ...AggregateFunc) *KeywordWeakSelect {
	return kwq.Select().Aggregate(fns...)
}

func (kwq *KeywordWeakQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range kwq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, kwq); err != nil {
				return err
			}
		}
	}
	for _, f := range kwq.ctx.Fields {
		if !keywordweak.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if kwq.path != nil {
		prev, err := kwq.path(ctx)
		if err != nil {
			return err
		}
		kwq.sql = prev
	}
	return nil
}

func (kwq *KeywordWeakQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*KeywordWeak, error) {
	var (
		nodes = []*KeywordWeak{}
		_spec = kwq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*KeywordWeak).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &KeywordWeak{config: kwq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, kwq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (kwq *KeywordWeakQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := kwq.querySpec()
	_spec.Node.Columns = kwq.ctx.Fields
	if len(kwq.ctx.Fields) > 0 {
		_spec.Unique = kwq.ctx.Unique != nil && *kwq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, kwq.driver, _spec)
}

func (kwq *KeywordWeakQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(keywordweak.Table, keywordweak.Columns, sqlgraph.NewFieldSpec(keywordweak.FieldID, field.TypeString))
	_spec.From = kwq.sql
	if unique := kwq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if kwq.path != nil {
		_spec.Unique = true
	}
	if fields := kwq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, keywordweak.FieldID)
		for i := range fields {
			if fields[i] != keywordweak.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := kwq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := kwq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := kwq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := kwq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (kwq *KeywordWeakQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(kwq.driver.Dialect())
	t1 := builder.Table(keywordweak.Table)
	columns := kwq.ctx.Fields
	if len(columns) == 0 {
		columns = keywordweak.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if kwq.sql != nil {
		selector = kwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if kwq.ctx.Unique != nil && *kwq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range kwq.predicates {
		p(selector)
	}
	for _, p := range kwq.order {
		p(selector)
	}
	if offset := kwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := kwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// KeywordWeakGroupBy is the group-by builder for KeywordWeak entities.
type KeywordWeakGroupBy struct {
	selector
	build *KeywordWeakQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (kwgb *KeywordWeakGroupBy) Aggregate(fns ...AggregateFunc) *KeywordWeakGroupBy {
	kwgb.fns = append(kwgb.fns, fns...)
	return kwgb
}

// Scan applies the selector query and scans the result into the given value.
func (kwgb *KeywordWeakGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, kwgb.build.ctx, "GroupBy")
	if err := kwgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KeywordWeakQuery, *KeywordWeakGroupBy](ctx, kwgb.build, kwgb, kwgb.build.inters, v)
}

func (kwgb *KeywordWeakGroupBy) sqlScan(ctx context.Context, root *KeywordWeakQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(kwgb.fns))
	for _, fn := range kwgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*kwgb.flds)+len(kwgb.fns))
		for _, f := range *kwgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*kwgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kwgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// KeywordWeakSelect is the builder for selecting fields of KeywordWeak entities.
type KeywordWeakSelect struct {
	*KeywordWeakQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (kws *KeywordWeakSelect) Aggregate(fns ...AggregateFunc) *KeywordWeakSelect {
	kws.fns = append(kws.fns, fns...)
	return kws
}

// Scan applies the selector query and scans the result into the given value.
func (kws *KeywordWeakSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, kws.ctx, "Select")
	if err := kws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KeywordWeakQuery, *KeywordWeakSelect](ctx, kws.KeywordWeakQuery, kws, kws.inters, v)
}

func (kws *KeywordWeakSelect) sqlScan(ctx context.Context, root *KeywordWeakQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(kws.fns))
	for _, fn := range kws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*kws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
